---
sidebar_position: 21
---

# 文章置顶

通过数字来指定文章 front-matter 中的 `sticky` 属性对文章进行置顶，数字越大置顶优先级越高。数字都需要大于 `0`。

```yml
---
title: 我的一篇文章
sticky: 1
---
```

## 使用说明

- `sticky` 值必须为大于 `0` 的数字
- 多篇文章同时设置 `sticky` 时，按数值从大到小排序
- 未设置 `sticky` 的文章按发布时间倒序排列在已置顶文章之后
